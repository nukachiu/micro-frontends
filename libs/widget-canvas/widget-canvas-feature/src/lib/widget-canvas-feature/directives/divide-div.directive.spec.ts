import { DivideDivDirective } from './divide-div.directive';

describe('DivideDivDirective', () => {
  it('should create an instance', () => {
    const directive = new DivideDivDirective();
    expect(directive).toBeTruthy();
  });
});
